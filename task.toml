artifacts = ["/app/report.json"]
[task]
name = "dynamo/log-report"
description = "Parse an Apache-style access log into a small JSON summary report."

[metadata]
category = "data_processing_and_etl"
subcategory = "text_processing"
task_objective = "transform"
artifact_type = "generated_output_artifact"
expert_time_estimate_hours = 0.3
model_tested = "gpt_5_4"
agent_tested = "terminus_2"
difficulty = "easy"
difficulty_explanation = "Parse a small Apache access log and generate summary statistics."
solution_explanation = "Read the access log, count requests and unique IPs, then output a JSON report."
verification_explanation = "Verify the generated JSON report matches the expected summary."

[verifier]
timeout_sec = 120

[agent]
timeout_sec = 120

[environment]
allow_internet = true
mcp_servers = []